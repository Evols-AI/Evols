# Evols Backend Requirements - Consolidated
# Python 3.13 Compatible
#
# INSTALLATION INSTRUCTIONS:
# 1. Install core dependencies: pip install -r requirements.txt
# 2. For development: uncomment the [DEVELOPMENT] section below
# 3. For local ML features: uncomment the [HEAVY ML] section below
# 4. LLM providers are all optional - only install what you use

# ============================================================================
# CORE FRAMEWORK & WEB SERVER (Required)
# ============================================================================
fastapi>=0.115.0    # 0.109.2 pulled starlette 0.36.3 (CVE-2024-47874, CVE-2025-54121)
uvicorn[standard]>=0.27.1
python-multipart>=0.0.26  # 0.0.9 had CVE-2024-53981, CVE-2026-24486, CVE-2026-40347

# ============================================================================
# DATABASE & STORAGE (Required)
# ============================================================================
sqlalchemy>=2.0.35  # Python 3.13 compatible
greenlet>=3.0.0  # Required for SQLAlchemy async operations
alembic==1.13.1
psycopg[binary]>=3.2.0  # Newer psycopg3 instead of psycopg2-binary
asyncpg==0.30.0  # Latest version with better Python 3.13 support
pgvector==0.2.5  # Vector store for embeddings

# ============================================================================
# AUTHENTICATION & SECURITY (Required)
# ============================================================================
python-jose[cryptography]>=3.4.0  # 3.3.0 had PYSEC-2024-232, PYSEC-2024-233
passlib[bcrypt]==1.7.4
python-dotenv>=1.2.2  # 1.0.1 had CVE-2026-28684
pydantic>=2.10.0  # Latest version with Python 3.13 support
pydantic-settings>=2.7.0
pydantic-extra-types>=2.10.0
email-validator==2.1.0

# ============================================================================
# AI/ML - LLM PROVIDERS
# litellm provides a unified interface to 100+ providers.
# boto3/anthropic/openai are still needed: boto3 for Bedrock (no REST API),
# anthropic for the MCP SDK dependency chain, openai for embeddings fallback.
# ============================================================================

litellm>=1.83.14  # Unified LLM provider abstraction.
                  # DO NOT lower this pin — 1.82.7 and 1.82.8 contain a supply chain backdoor
                  # (injected into wheel files; scans env vars for API keys, installs C2 persistence).
                  # 1.83.x also fixes CVE-2026-42208 (SQL injection) and CVE-2026-42271 (MCP command injection).
                  # 1.83.14+ requires python-dotenv==1.2.2 (1.83.13 wrongly pinned 1.0.1 which has CVE-2026-28684).

# AWS Bedrock — boto3 required because Bedrock has no OpenAI-compatible REST endpoint;
# litellm calls boto3 internally for bedrock/* models.
boto3>=1.34.34
botocore>=1.34.34

# openai sdk kept for embeddings (text-embedding-3-large) and instructor
openai>=2.0.0   # litellm>=1.83.7 requires openai 2.x

# anthropic sdk kept for MCP python-sdk dependency chain
anthropic>=0.18.1

# Structured LLM outputs (OpenAI/Azure only)
instructor>=1.7.0  # 1.7+ supports openai 2.x (1.6.4 required openai<2)

# ============================================================================
# DATA PROCESSING (Required)
# ============================================================================
openpyxl==3.1.2  # Excel file processing
numpy>=1.26.4  # Mathematical operations
pandas>=2.2.0  # Data manipulation
python-docx==1.1.2  # Word document parsing
PyPDF2==3.0.1  # PDF text extraction

# ============================================================================
# HTTP CLIENT & WEB SCRAPING (Required)
# ============================================================================
httpx>=0.28.1  # Async HTTP client (0.28.1+ required by litellm>=1.83.7)
aiofiles==23.2.1  # Async file operations
beautifulsoup4==4.12.3  # HTML parsing

# ============================================================================
# UTILITIES (Required)
# ============================================================================
python-slugify==8.0.3  # URL-friendly string generation
cryptography>=42.0.0  # AES-256-GCM token encryption (already pulled transitively; pinned here for explicitness)

# ============================================================================
# DATA INTEGRATIONS (Required)
# dlt: in-process connector framework for Slack, Notion, Zendesk, GitHub, Salesforce
# msal + msgraph-sdk-python: Microsoft OAuth + Graph API for Outlook / Teams
# ============================================================================
dlt[duckdb]>=1.0.0          # Core dlt; duckdb backend for staging buffers
msal>=1.28.0                  # Microsoft Authentication Library — PKCE + refresh token management
msgraph-sdk>=1.0.0            # Microsoft Graph SDK — Outlook mail, Teams messages, calendar

# ============================================================================
# MONITORING & BACKGROUND TASKS (Required)
# ============================================================================
loguru==0.7.2  # Structured logging
prometheus-client==0.19.0  # Metrics collection
APScheduler==3.10.4  # Task scheduling
celery[redis]==5.3.6  # Durable task queue
redis==5.0.1  # Redis client for Celery

# ============================================================================
# HEAVY ML DEPENDENCIES (Optional - uncomment if needed)
# Warning: These packages are large and memory-intensive
# Only enable if you need local embeddings or advanced clustering
# ============================================================================

# Transformers and embeddings (large downloads ~2GB+)
# Used as fallback when tenant LLM provider doesn't support embeddings (e.g. Google Gemini, Anthropic)
sentence-transformers>=2.3.1
# torch==2.2.0  # installed transitively by sentence-transformers

# Clustering algorithms
# scikit-learn==1.4.0
# hdbscan==0.8.33

# ============================================================================
# DEVELOPMENT DEPENDENCIES (Optional - uncomment for development)
# Only needed if you're developing or contributing to the codebase
# ============================================================================

# Testing
# pytest==7.4.4
# pytest-asyncio==0.23.3

# Code formatting and linting
# black==24.1.1
# flake8==7.0.0
# mypy==1.8.0