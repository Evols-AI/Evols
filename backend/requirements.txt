# Evols Backend Requirements - Consolidated
# Python 3.13 Compatible
#
# INSTALLATION INSTRUCTIONS:
# 1. Install core dependencies: pip install -r requirements.txt
# 2. For development: uncomment the [DEVELOPMENT] section below
# 3. For local ML features: uncomment the [HEAVY ML] section below
# 4. LLM providers are all optional - only install what you use

# ============================================================================
# CORE FRAMEWORK & WEB SERVER (Required)
# ============================================================================
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# ============================================================================
# DATABASE & STORAGE (Required)
# ============================================================================
sqlalchemy>=2.0.35  # Python 3.13 compatible
greenlet>=3.0.0  # Required for SQLAlchemy async operations
alembic==1.13.1
psycopg[binary]>=3.2.0  # Newer psycopg3 instead of psycopg2-binary
asyncpg==0.30.0  # Latest version with better Python 3.13 support
pgvector==0.2.5  # Vector store for embeddings

# ============================================================================
# AUTHENTICATION & SECURITY (Required)
# ============================================================================
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
pydantic>=2.10.0  # Latest version with Python 3.13 support
pydantic-settings>=2.7.0
pydantic-extra-types>=2.10.0
email-validator==2.1.0

# ============================================================================
# AI/ML - LLM PROVIDERS (Optional - install only what you use)
# Comment out providers you don't need to reduce dependencies
# ============================================================================

# AWS Bedrock (uses your AWS credentials, no extra API keys needed)
boto3>=1.34.34
botocore>=1.34.34

# OpenAI (if using OpenAI directly)
openai>=1.54.0  # Latest with native async support

# Anthropic (if using Anthropic directly)
anthropic==0.18.1

# Google Generative AI (if using Google Gemini)
google-generativeai>=0.8.0

# Structured LLM outputs
instructor==1.6.4

# ============================================================================
# DATA PROCESSING (Required)
# ============================================================================
openpyxl==3.1.2  # Excel file processing
numpy>=1.26.4  # Mathematical operations
pandas>=2.2.0  # Data manipulation
python-docx==1.1.2  # Word document parsing
PyPDF2==3.0.1  # PDF text extraction

# ============================================================================
# HTTP CLIENT & WEB SCRAPING (Required)
# ============================================================================
httpx==0.26.0  # Async HTTP client
aiofiles==23.2.1  # Async file operations
beautifulsoup4==4.12.3  # HTML parsing

# ============================================================================
# UTILITIES (Required)
# ============================================================================
python-slugify==8.0.3  # URL-friendly string generation

# ============================================================================
# MONITORING & BACKGROUND TASKS (Required)
# ============================================================================
loguru==0.7.2  # Structured logging
prometheus-client==0.19.0  # Metrics collection
APScheduler==3.10.4  # Task scheduling
celery[redis]==5.3.6  # Durable task queue
redis==5.0.1  # Redis client for Celery

# ============================================================================
# HEAVY ML DEPENDENCIES (Optional - uncomment if needed)
# Warning: These packages are large and memory-intensive
# Only enable if you need local embeddings or advanced clustering
# ============================================================================

# Transformers and embeddings (large downloads ~2GB+)
# sentence-transformers==2.3.1
# torch==2.2.0

# Clustering algorithms
# scikit-learn==1.4.0
# hdbscan==0.8.33

# ============================================================================
# DEVELOPMENT DEPENDENCIES (Optional - uncomment for development)
# Only needed if you're developing or contributing to the codebase
# ============================================================================

# Testing
# pytest==7.4.4
# pytest-asyncio==0.23.3

# Code formatting and linting
# black==24.1.1
# flake8==7.0.0
# mypy==1.8.0